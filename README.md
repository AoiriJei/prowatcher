
  # pro watcher

  This is a code bundle for pro watcher. The original project is available at https://www.figma.com/design/yV03geqbaGgxiY9GSxK1Lf/pro-watcher.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  